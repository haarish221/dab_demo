resources:
  pipelines:
    demo_pipeline:
      name: demo_pipeline
      libraries:
        - glob:
            include: ../../src/pipelines/demo_pipeline/transformations/**
      schema: bronze
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalogue_name}
      serverless: true
      root_path: ../../src/pipelines/demo_pipeline
